// Main
if (!dice) {
    var dice = {};
}
dice.ui = new function() {
    var self = this;

    var diceView = {
        playerContainerId: "dice-scoreboard"
    };

    this.addPlayerContainer = function() {
        var scoreboard = $("#" + diceView.playerContainerId);
        var playerContainer = $("<div>");
        playerContainer.addClass(c)
    }
}